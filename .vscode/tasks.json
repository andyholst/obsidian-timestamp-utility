{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Makefile",
            "type": "shell",
            "command": "make",
            "args": [],  // Add specific targets here if needed, e.g., ["build-app"]
            "options": {
                "env": {
                    "PATH": "${env:PATH}:/usr/local/bin"
                }
            },
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
