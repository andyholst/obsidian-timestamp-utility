# Tasks — doc-sync-tests

- [x] 1.1 Create `tests/fixtures/check_docs_sync/<scenario>/` as REAL copies of the 3 B8 sync `.md` files only (AGENTS.md, hermes/skills/openspec-loop-harness.md, docs/openspec-engineering-loop-harness.md) — NO Makefile / run-loop-harness.sh / check-docs-sync.py copies
- [x] 1.2 Scenarios: `in_sync` / `in_sync_ascii` / `in_sync_en_dash` (aligned → PASS, glyph-tolerant) + `drift_b_range_low` (AGENTS.md B1-B21) + `drift_reorder` (secondary mention reordered → no false positive). Drift applied to the `.md` copy only.
- [x] 2.1 Write `tests/test_check_docs_sync.py`: load `scripts/check-docs-sync.py` via importlib; each test builds a TEMP repo root = freshly-copied real Makefile + run-loop-harness.sh (so the gate's full 5-file check passes on those) + the 3 fixture `.md` files, then assert `check_docs_sync()` verdicts against the `.md` drift only
- [x] 2.2 Pure-function tests: `normalize` (glyph/backtick/paren collapse), `ordered_stages_present` (canonical chain present/absent/reordered), `b_range_ok` (B1-B25 / en-dash / parenthetical / too-low)
- [x] 3.1 FIXTURE INTEGRITY tests: aligned scenarios are BYTE-IDENTIAL to the real repo `.md`; drifted scenarios differ ONLY by their declared one-token mutation (proves fixtures are real copies, not fakes)
- [x] 3.2 UNMODIFIED-FILE CLOSENESS tests: in each drift scenario, EVERY `.md` EXCEPT the deliberately drifted one is byte-identical to the real repo file — the fixture is "very close" to the asserted docs, drift isolated to exactly one file
- [x] 3.4 SEMANTIC ACCURACY THRESHOLD: every fixture has a ground-truth label (aligned→PASS / drifted→FAIL); gate accuracy over the labelled corpus MUST be >= 95% (held at 100%); misclassified scenarios reported explicitly — the agentic "reasoning" bar
- [x] 3.2 DISCRIMINATION / mutation tests: green fixture flips RED when drifted; red fixture flips GREEN when repaired AND the repaired temp file is byte-equal to the real source; a second independent drift axis also flips — proving the suite is ACCURATE, not tautological
- [x] 4.1 Add `make test-check-docs-sync` that runs INSIDE the `unit-test-agents` docker container via `$(call docker_run, docker compose ... run --rm unit-test-agents ...)` (NO host python3 call); the gate script is mounted from host at `/project`. Add `make check-docs-sync-and-test`
- [x] 5.1 Wire `make test-check-docs-sync` + `make check-docs-sync` as PRE-FLIGHT 0 at the start of `scripts/run-loop-harness.sh` (abort run on non-zero)
- [x] 6.1 TEST: `make test-check-docs-sync` — expect 24 passing tests (24 on host, 24 in container) covering real-fixture semantics + accuracy layer
- [x] 6.2 TEST: `make check-docs-sync` — expect PASS (current tree aligned)
- [x] 7.1 `openspec validate doc-sync-tests` passes
- [x] 7.2 B8-sync: runner/AGENTS.md/Makefile all list the pre-flight + final doc-sync gate consistently
- [x] 8.1 VERIFY: `make loop-harness` (host) runs PRE-FLIGHT 0 first and proceeds only when green — CONFIRMED on host: `run-loop-harness.sh --hermetic` prints `=== PRE-FLIGHT 0: check-docs-sync unit tests + live gate ===` at line 1, `PRE-FLIGHT 0 PASS` at line 15, THEN stages run (loop-collect/loop-ts-floor/loop-unit all PASS, loop_exit=0). Abort path also verified by code (exit 1 before stage loop if either red).
- [x] 9.1 Add `make regen-doc-sync-fixtures` (committed `scripts/regen_doc_sync_fixtures.py`): rebuilds the `.md` fixtures from the CURRENT real docs, re-applies drifts with **anchor assertions** so a doc restructure can't silently neutralize a drift scenario; then runs `make test-check-docs-sync` to prove the rebuild is correct
- [x] 10.1 Harden: gate contract now DERIVED from source-of-truth (`run-loop-harness.sh` STAGES + max-B in narrative .md), not hardcoded constants — doc evolution (B26, new stage) needs NO constant edit, only `make regen-doc-sync-fixtures`
- [x] 10.2 Harden: drift anchors DERIVED in `scripts/regen_doc_sync_fixtures.py` (_derive_b_range_drift lowers current max-B; anchor-check fails loudly if doc restructure drops the phrase) + test imports the same module so anchors can't disagree (single source of truth)
- [x] 10.4 Harden gate: catches MISSING MAKE TARGETS — `missing_make_targets()` checks every canonical stage exists as a real `name:` Makefile target (derived from runner STAGES), so deleting `loop-e2e:` (target gone but chain comment remains) now goes RED with "missing make target(s): loop-e2e" (proven by probe: silent FAIL before, RED after)
- [x] 10.5 New NEGATIVE tests cover ALL 5 sync files, not just `.md`: `test_b_range_low_red_when_any_single_file_drifts` + `test_stage_removed_red_when_any_single_file_drifts` (AGENTS.md / hermes skill / harness doc) + `test_missing_make_target_red` + `test_make_target_present_stays_green` (Makefile target presence). Gate reacts RED and names exactly the offending file/command when a B-step/stage/make-target is removed
