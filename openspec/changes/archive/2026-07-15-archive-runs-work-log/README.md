# archive-runs-work-log

Make the Phase-7 work-log (record-work) run automatically as part of phase7-archive, so every archived OpenSpec change emits its agent-wiki entry without a separate manual step.
