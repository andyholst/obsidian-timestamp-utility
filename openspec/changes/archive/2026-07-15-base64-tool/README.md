# base64-tool

Add a base64 encode/decode Obsidian command+modal: open a modal via the command palette, enter a message, and encode (or decode) it with base64, showing the result in the modal. Generated by the agentic pipeline via the deterministic contract floor.
