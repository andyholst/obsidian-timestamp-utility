# Tasks

- [x] 1.1 Implement the core change for `ztest-archive-order` (TODO: concrete, verifiable step)
- [x] 2.1 Verify: run the relevant gate(s) — e.g. `make build-app` / `make test-app` / `make loop-unit`
- [x] 2.2 TODO: add/adjust tests that prove the behaviour
- [x] 3.1 B8-sync: update AGENTS.md + openspec-loop-harness skill if behaviour/docs changed
- [x] 4.1 `openspec validate ztest-archive-order` passes
