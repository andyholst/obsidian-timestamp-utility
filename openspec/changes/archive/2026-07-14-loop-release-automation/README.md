# loop-release-automation

When the loop-harness is green after new TS code/tests are generated, automatically bump the Obsidian plugin version (manifest.json+package.json+versions.json), update README release notes, generate CHANGELOG, squash commits, and tag the release
