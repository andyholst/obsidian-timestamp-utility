# changelog-commit-alignment

Regenerate CHANGELOG.md so its Unreleased section reflects the squashed local commit(s), scrub test-leak commits, and stays aligned with git log after make squash-commits
